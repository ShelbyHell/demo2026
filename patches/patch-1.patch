diff --git a/index-frr.html b/index-frr.html
index 25e8528..d6beaa7 100644
--- a/index-frr.html
+++ b/index-frr.html
-# Включить IP-форвардинг
-[root@hq-rtr ~]# nano /etc/net/sysctl.conf</code></pre>
-<pre><div class="code-header"><div class="dots"><span></span><span></span><span></span></div><span class="lang">diff</span></div><code class="language-diff">- net.ipv4.ip_forward = 0
-+ net.ipv4.ip_forward = 1</code></pre>
+<p>Откройте файл для редактирования:</p>
+<pre><code class="language-bash">nano /etc/net/sysctl.conf</code></pre>
+<p>Измените или добавьте строку:</p>
+<pre><code class="language-diff">- net.ipv4.ip_forward = 0
++ net.ipv4.ip_forward = 1</code></pre>

-[root@hq-rtr ~]# mkdir -p /etc/net/ifaces/enp6s21.100
-[root@hq-rtr ~]# nano /etc/net/ifaces/enp6s21.100/options
-<pre><div class="code-header"><div class="dots"><span></span><span></span><span></span></div><span class="lang">plaintext</span></div><code class="language-plaintext">TYPE=vlan
-BOOTPROTO=static
-CONFIG_WIRELESS=no
-SYSTEMD_WAITONLINE=no
-VID=100
-HOST=enp6s21</code></pre>
+<pre><code class="language-bash">mkdir -p /etc/net/ifaces/enp6s21.100
+nano /etc/net/ifaces/enp6s21.100/options</code></pre>
+<p>Вставьте в файл:</p>
+<pre><code class="language-plaintext">TYPE=vlan
+BOOTPROTO=static
+CONFIG_WIRELESS=no
+SYSTEMD_WAITONLINE=no
+VID=100
+HOST=enp6s21</code></pre>
-[root@hq-rtr ~]# mkdir -p /etc/net/ifaces/enp6s21.200
-[root@hq-rtr ~]# nano /etc/net/ifaces/enp6s21.200/options
-<pre><div class="code-header"><div class="dots"><span></span><span></span><span></span></div><span class="lang">plaintext</span></div><code class="language-plaintext">TYPE=vlan
-BOOTPROTO=static
-CONFIG_WIRELESS=no
-SYSTEMD_WAITONLINE=no
-VID=200
-HOST=enp6s21</code></pre>
+<pre><code class="language-bash">mkdir -p /etc/net/ifaces/enp6s21.200
+nano /etc/net/ifaces/enp6s21.200/options</code></pre>
+<p>Вставьте в файл:</p>
+<pre><code class="language-plaintext">TYPE=vlan
+BOOTPROTO=static
+CONFIG_WIRELESS=no
+SYSTEMD_WAITONLINE=no
+VID=200
+HOST=enp6s21</code></pre>
-[root@hq-rtr ~]# mkdir -p /etc/net/ifaces/enp6s21.999
-[root@hq-rtr ~]# nano /etc/net/ifaces/enp6s21.999/options
-<pre><div class="code-header"><div class="dots"><span></span><span></span><span></span></div><span class="lang">plaintext</span></div><code class="language-plaintext">TYPE=vlan
-BOOTPROTO=static
-CONFIG_WIRELESS=no
-SYSTEMD_WAITONLINE=no
-VID=999
-HOST=enp6s21</code></pre>
+<pre><code class="language-bash">mkdir -p /etc/net/ifaces/enp6s21.999
+nano /etc/net/ifaces/enp6s21.999/options</code></pre>
+<p>Вставьте в файл:</p>
+<pre><code class="language-plaintext">TYPE=vlan
+BOOTPROTO=static
+CONFIG_WIRELESS=no
+SYSTEMD_WAITONLINE=no
+VID=999
+HOST=enp6s21</code></pre>
-# Включить IP-форвардинг
-[root@br-rtr ~]# nano /etc/net/sysctl.conf</code></pre>
-<pre><div class="code-header"><div class="dots"><span></span><span></span><span></span></div><span class="lang">diff</span></div><code class="language-diff">- net.ipv4.ip_forward = 0
-+ net.ipv4.ip_forward = 1</code></pre>
+<p>Откройте файл для редактирования:</p>
+<pre><code class="language-bash">nano /etc/net/sysctl.conf</code></pre>
+<p>Измените или добавьте строку:</p>
+<pre><code class="language-diff">- net.ipv4.ip_forward = 0
++ net.ipv4.ip_forward = 1</code></pre>
-      <pre><div class="code-header"><div class="dots"><span></span><span></span><span></span></div><span class="lang">bash</span><button class="copy-btn" onclick="copyCode(this)">copy</button></div><c[...]
-[root@hq-rtr ~]# nano /etc/frr/daemons</code></pre>
-      <pre><div class="code-header"><div class="dots"><span></span><span></span><span></span></div><span class="lang">diff</span></div><code class="language-diff">- zebra=no
-+ zebra=yes
-
-- ospfd=no
-+ ospfd=yes</code></pre>
+<p>Откройте файл для редактирования:</p>
+<pre><code class="language-bash">nano /etc/frr/daemons</code></pre>
+<p>Измените параметры для запуска демонов zebra и ospfd:</p>
+<pre><code class="language-diff">- zebra=no
++ zebra=yes
+
+- ospfd=no
++ ospfd=yes</code></pre>
-[root@hq-rtr ~]# nano /etc/net/ifaces/tunnel0/options
-<pre><div class="code-header"><div class="dots"><span></span><span></span><span></span></div><span class="lang">plaintext</span></div><code class="language-plaintext">TYPE=iptun
-TUNTYPE=gre
-TUNLOCAL=172.16.1.2
-TUNREMOTE=172.16.2.2
-TTLMODE=fixed
-TTL=255
-BOOTPROTO=static</code></pre>
+<p>Откройте файл для редактирования:</p>
+<pre><code class="language-bash">nano /etc/net/ifaces/tunnel0/options</code></pre>
+<p>Вставьте в файл:</p>
+<pre><code class="language-plaintext">TYPE=iptun
+TUNTYPE=gre
+TUNLOCAL=172.16.1.2
+TUNREMOTE=172.16.2.2
+TTLMODE=fixed
+TTL=255
+BOOTPROTO=static</code></pre>
-[root@br-rtr ~]# nano /etc/net/ifaces/tunnel0/options
-<pre><div class="code-header"><div class="dots"><span></span><span></span><span></span></div><span class="lang">plaintext</span></div><code class="language-plaintext">TYPE=iptun
-TUNTYPE=gre
-TUNLOCAL=172.16.2.2
-TUNREMOTE=172.16.1.2
-TTLMODE=fixed
-TTL=255
-BOOTPROTO=static</code></pre>
+<p>Откройте файл для редактирования:</p>
+<pre><code class="language-bash">nano /etc/net/ifaces/tunnel0/options</code></pre>
+<p>Вставьте в файл:</p>
+<pre><code class="language-plaintext">TYPE=iptun
+TUNTYPE=gre
+TUNLOCAL=172.16.2.2
+TUNREMOTE=172.16.1.2
+TTLMODE=fixed
+TTL=255
+BOOTPROTO=static</code></pre>
-[root@hq-rtr ~]# nano /etc/openssh/sshd_config
-[root@hq-rtr ~]# echo "AllowUsers sshuser"         >> /etc/openssh/sshd_config
-[root@hq-rtr ~]# echo "MaxAuthTries 2"             >> /etc/openssh/sshd_config
-[root@hq-rtr ~]# echo "Banner /etc/openssh/banner" >> /etc/openssh/sshd_config
-[root@hq-rtr ~]# echo "Authorized access only!" > /etc/openssh/banner
+<p>Откройте файл для редактирования:</p>
+<pre><code class="language-bash">nano /etc/openssh/sshd_config</code></pre>
+<p>Добавьте в конец файла:</p>
+<pre><code class="language-plaintext">Port 2026
+AllowUsers sshuser
+MaxAuthTries 2
+Banner /etc/openssh/banner</code></pre>
+<p>А затем создайте файл баннера:</p>
+<pre><code class="language-bash">echo "Authorized access only!" > /etc/openssh/banner</code></pre>
-<p>Найти строку <code>DHCPDARGS</code> и заменить:</p>
-<pre><div class="code-header"><div class="dots"><span></span><span></span><span></span></div><span class="lang">diff</span></div><code class="language-diff">- DHCPDARGS=
-+ DHCPDARGS="enp6s21.200"</code></pre>
+<p>Откройте файл для редактирования:</p>
+<pre><code class="language-bash">nano /etc/sysconfig/dhcpd</code></pre>
+<p>Найдите строку DHCPDARGS и измените на:</p>
+<pre><code class="language-diff">- DHCPDARGS=
++ DHCPDARGS="enp6s21.200"</code></pre>
-[root@br-srv ~]# nano /etc/resolv.conf
-<pre><div class="code-header"><div class="dots"><span></span><span></span><span></span></div><span class="lang">plaintext</span></div><code class="language-plaintext">search au-team.irpo
-nameserver 127.0.0.1</code></pre>
+<p>Откройте файл для редактирования:</p>
+<pre><code class="language-bash">nano /etc/resolv.conf</code></pre>
+<p>Вставьте в файл:</p>
+<pre><code class="language-plaintext">search au-team.irpo
+nameserver 127.0.0.1</code></pre>